/***
 *
 *
 * ? What defines a pure function?
 *
 */

// 1. Total
sub = (i) => {
  return 100;
};

// 2. Detriministic
const getDifferenceInTime = (now, value) => {
  return now - value;
};

// 3. No Side Effects (Affecting the Observable World)

const query = (window) => window.location.search.split('?').concat([]); // Function or Not?

const App = (jQ) => {
  return {
    getElement: (tag) => {
      return jQ.getTag(tage);
    },
  };
};


const APP_TIME = 40000;


const calcualteTimeDiff = () => {
    APP_TIME
}




